# Delivery Rate Forecast System

A Streamlit app for forecasting delivery rate using SKU, seller, courier, validation, WhatsApp response, timing factors, volume effect, calibration, and order-group logic.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py